此仓库将被fork并用作毕业设计。相关计划如下：

1.前端推倒重写，不用solidjs，用 react + shadcn + tailwindcss + vite

2.简洁前端 + genUI
使用 agent tool 来让前端自动生成UI，给genAI的tool加一个专门的js函数调用，该函数用来再次调用llm发送消息。后端生成UI代码，然后前端执行。这些专门的JS函数调用放在按钮的onclick事件中，用户点击后，实现自动发送新的消息。agent得知消息的回复内容，再试着做出回复，执行其他工具调用，甚至更新UI。

3.推荐气泡
每次都得用户手动输入回复太麻烦，可以让agent推荐一些回复，用户可以选择。这些回复以气泡形式浮动在输入框上方，可以触发快速回复。


4.电商与对话联动
结合电商能力，在对话中穿插系统信息，实现「AI 主动通知」，且用户立刻在对话里看到 AI 的回复（不依赖刷新/下次打开）。
- 订单/支付等事件发生时，向该用户当前（或指定）会话追加一条 system message（如订单号、金额、状态），再调一次 completion 取 AI 的回复，append 进对话并写回。通过推送把这条新消息立刻推到前端展示。
- 「最后一条是 assistant 时再发 completion」可让 AI 连发两条，需在实际模型上试一轮确认行为。
- **单 SSE 接收、发消息用 HTTP**：进房间建一条长连 SSE，专用于接收（server→client）。用户发消息走普通 HTTP（如 POST /chat），不建新连接。后端把 completion 回复（流式）和订单完成等推送，都通过这条唯一的 SSE 下发。前端只维护一个 EventSource，按 event/data 类型分支。这样仅一条连接，比双 SSE 简单，也比上 WebSocket 更轻。

5.激进的用户验证
采用web authn
目前mongo数据库user的id是邮箱，预计取消邮箱，服务器不保留任何用户信息，比如邮箱和手机号。
mongo id换成 mongo自己生成 的object id，新增一个公钥字段
前端使用web authn技术，获取通行凭证，公钥则发给后端保存。
消灭密码，使用通行凭证。
消灭验证码，邮箱，手机号，人机识别，使用POW，算力验证。大概伪代码
p, _ := rand.Prime(rand.Reader, 30)
q, _ := rand.Prime(rand.Reader, 30)
n := new(big.Int).Mul(p, q).Uint64()
出这个难题，然后通过现成的tokens包把问题生成token发给前端
token包含问题明文，和明文hmac后的签名，前端拿到token后计算难题，再把答案和token发给后端，后端验签确定是自己出的题目，然后判断答案，正确则批准登录
tokens包需要优化，目前payload是写死的，用泛型实现自定义负载

消灭UI注册，只有登录。使用web authn，服务器发随机数用公钥加密，挑战客户端，考虑使用 go-webauthn/webauthn  初次登陆（如果数据库没有用户记录），需要工作量证明来完成注册

